<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luizeduardo.controledieta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dieta"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".controllers.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.NovoUsuario" />
        <activity android:name=".controllers.DadosUsuario" />
        <activity android:name=".controllers.ControleDieta" />
    </application>
</manifest>